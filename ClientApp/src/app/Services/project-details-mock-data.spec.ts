import { ProjectDetailsMockData } from './project-details-mock-data';

describe('ProjectDetailsMockData', () => {
  it('should create an instance', () => {
    expect(new ProjectDetailsMockData()).toBeTruthy();
  });
});
